import React, { Component } from "react";

import MapItem from "../../components/MapItem/MapItem"

class MapBuild extends Component {
    render() {
        return (
            <MapItem />
        );
    }
}

export default MapBuild;